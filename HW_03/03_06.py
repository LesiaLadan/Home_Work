# Реалізуйте функцію, яка приймає число і виводить "Парне", якщо число парне,
# і "Непарне", якщо непарне.
# Створіть функцію, яка приймає список чисел і повертає новий список,
#  що містить тільки парні числа.


def even(a: int) -> str:
    return "Even" if a % 2 == 0 else "Odd"


print(even(2858))


def even_list(a: list) -> list:
    return [x for x in a if x % 2 == 0]


print(even_list([1, 2, 3, 4, 5, 6]))
